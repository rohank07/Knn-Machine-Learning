
public class UserPrompt 
{
	/*
	 * What this class should do:
	 * 	Prompt user for column names, distance metrics (enums) and whether the feature is continuous or a label
	 * 	Store the column headings
	 * 	Prompt user to create a row
	 * 	Store row information
	 * 	Provide the column headings to Table class
	 * 	Provide row to Table class
	 */
}
